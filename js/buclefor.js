'use strict'

//Bucle For
// Bucle es una estructura de control que se repite varias veces

var numero = 100;

for(var i = 0; i <= numero; i++)// la estructura dicta que siempre hay una variable a comparar, una condicion y un contador de repeticiones
{
	console.log("vamos por el número: "+i);
	debugger; // esta instruccion funciona para abrir la herramienta debugger en el navegador y ejecutar el codigo linea por linea
}

